apiVersion: v1
kind: Service
metadata:
  name: service-lb
spec:
  type: LoadBalancer
  selector:
    project: "roboshop"
    enironment: "dev"
    purpose: "LoadBalancer"
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30007