apiVersion: v1
kind: Pod
metadata:
  name: frontend
  namespace: roboshop
  labels: 
    project: roboshop
    environment: dev
spec:
  containers:
  - name: frontend
    image: nginx
    ports:
    - containerPort: 80