apiVersion: v1
kind: Service
metadata:
  name: service
spec:
  selector:
    project: "roboshop"
    enironment: "dev"
    purpose: "service"
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80